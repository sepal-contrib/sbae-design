# Core Solara dependencies
solara>=1.0.0
solara-ui>=1.0.0

# Geospatial processing
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.4.0
fiona>=1.8.0

# Scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
ipecharts>=1.0.0

# Mapping / UI framework: pysepal (formerly sepal-ui) — see the git dep below.
# Provides MapApp, AoiView, FileInput, and the notification system.

# Optional: For enhanced file format support
h5netcdf>=1.2.0  # For NetCDF files
xarray>=2023.1.0  # For multidimensional arrays

# Development and testing (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0


# pysepal is the renamed sepal_ui; `main` includes the notification system.
# The `sepal_ui` import path still works via a compatibility shim.
git+https://github.com/openforis/pysepal.git
git+https://github.com/gee-community/pygaul.git
voila
rasterio<1.3.11 # Leave it here even if it's already in the pysepal requirements. Check https://github.com/openforis/sepal/issues/328
ipyvuetify


